{
    "name": "net-control-frontend",
    "version": "1.0.0",
    "license": "MIT",
    "scripts": {
        "start": "serve -s public -l 3000",
        "preinstall": "npm install -g serve",
        "build": "echo 'Building...' && cp -r src/* public/ || true",
        "test": "echo 'No tests specified'"
    },
    "dependencies": {
        "serve": "^14.2.0"
    },
    "engines": {
        "node": ">=16.0.0"
    }
}
